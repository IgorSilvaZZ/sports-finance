server:
  port: 8000
spring:
  application:
    name: responsible-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/responsible_service?useTimezone=true&serverTimezone=UTC
    username: dev
    password: admin123
  jackson:
    default-property-inclusion: non_null
    serialization:
      fail-on-empty-beans: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  messages:
    basename: messages
    encoding: ISO-8859-1
api:
  security:
    token:
      secret: ${JWT_SECRET:sports-finance-auth}